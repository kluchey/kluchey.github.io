/*
var main = function() {
	
});
};
*/

// $(document).ready(main);

$(document).ready(function(){
	$('#webpage').fadeIn(1000);
});
/*
	$(window).bind('scroll', function () {
    if ($(window).scrollTop() > 0) {
        $('div.header').css({
			"position": "fixed"
		});
    } else {
        $('div.header').css({
			"position": "static"
		});
	};
	});
	*/